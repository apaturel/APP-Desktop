package org.app.desktop.repository;

import org.app.desktop.model.WeeklyTrainingLoadModel;

public interface WeeklyTrainingLoadRepository {
    String save(WeeklyTrainingLoadModel weeklyTrainingLoad);
}
